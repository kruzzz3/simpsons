<div class="sub_navigation">
  <%= link_to(image_tag("icon_back_inv.png", :class => 'image-link') + raw('<span class="text-inv">Zurück</span>'), '/admin') %>
</div>
<div class="content">
	<h1>Import</h1>

	<% if notice %>
		<p class="notice"><%= notice %></p>
	<% end %>

	<%= render 'form' %>
</div>