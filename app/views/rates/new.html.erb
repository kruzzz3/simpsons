<div class="sub_navigation">
 	<%= link_to(image_tag("icon_back.png", :class => 'image-link') + raw('<span class="text">Zurück</span>'), '/rates') %>
</div>
<div class="content">
	<h1>Folge <%= @episode.number %> (<%= ApplicationHelper.get_number(@episode.season.number, @episode.sort) %>)</h1>
  	<h2><%= @episode.title %></h2>
  	<p><%= simple_format @episode.lead %></p>
  	<br>

	<%= render 'form' %>
</div>